import 'package:flutter/material.dart';

class ExampleSlideC extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Example Slide C"),
    );
  }
}