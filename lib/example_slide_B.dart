import 'package:flutter/material.dart';

class ExampleSlideB extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Example Slide B"),
    );
  }
}